language: java
jdk:
  - oraclejdk7
  - openjdk7

install:
  - mvn install -Dmaven.test.skip

script: 
  - mvn verify -Pcassandra-2.0 --quiet

notifications:
  irc: "irc.freenode.net#blueflood"
  email:
    recepients:
      - blueflood-discuss@googlegroups.com
