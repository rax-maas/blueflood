#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.9.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import TelescopeServer
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  void Publish( messages)'
  print '  void RepeatEvents( repeatEvents)'
  print '  void RemoveBoundChecks( removeBoundChecks)'
  print '  void UpdateCollectorTimeout(CollectorTimeout timeout)'
  print '  void TestCompileAlarm(string txnId, string query)'
  print '  void AddAlarm(string alarmId, string query)'
  print '  void RemoveAlarm(string alarmId)'
  print '  void TestCompileRegexes(string txnId,  regexes)'
  print '  string GetAlarm(string alarmId)'
  print '  string GetAlarmState(string alarmId)'
  print '  void UpdateAlarmDisabledState(string acctId, string entityId, string alarmId, bool disabled)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = TelescopeServer.Client(protocol)
transport.open()

if cmd == 'Publish':
  if len(args) != 1:
    print 'Publish requires 1 args'
    sys.exit(1)
  pp.pprint(client.Publish(eval(args[0]),))

elif cmd == 'RepeatEvents':
  if len(args) != 1:
    print 'RepeatEvents requires 1 args'
    sys.exit(1)
  pp.pprint(client.RepeatEvents(eval(args[0]),))

elif cmd == 'RemoveBoundChecks':
  if len(args) != 1:
    print 'RemoveBoundChecks requires 1 args'
    sys.exit(1)
  pp.pprint(client.RemoveBoundChecks(eval(args[0]),))

elif cmd == 'UpdateCollectorTimeout':
  if len(args) != 1:
    print 'UpdateCollectorTimeout requires 1 args'
    sys.exit(1)
  pp.pprint(client.UpdateCollectorTimeout(eval(args[0]),))

elif cmd == 'TestCompileAlarm':
  if len(args) != 2:
    print 'TestCompileAlarm requires 2 args'
    sys.exit(1)
  pp.pprint(client.TestCompileAlarm(args[0],args[1],))

elif cmd == 'AddAlarm':
  if len(args) != 2:
    print 'AddAlarm requires 2 args'
    sys.exit(1)
  pp.pprint(client.AddAlarm(args[0],args[1],))

elif cmd == 'RemoveAlarm':
  if len(args) != 1:
    print 'RemoveAlarm requires 1 args'
    sys.exit(1)
  pp.pprint(client.RemoveAlarm(args[0],))

elif cmd == 'TestCompileRegexes':
  if len(args) != 2:
    print 'TestCompileRegexes requires 2 args'
    sys.exit(1)
  pp.pprint(client.TestCompileRegexes(args[0],eval(args[1]),))

elif cmd == 'GetAlarm':
  if len(args) != 1:
    print 'GetAlarm requires 1 args'
    sys.exit(1)
  pp.pprint(client.GetAlarm(args[0],))

elif cmd == 'GetAlarmState':
  if len(args) != 1:
    print 'GetAlarmState requires 1 args'
    sys.exit(1)
  pp.pprint(client.GetAlarmState(args[0],))

elif cmd == 'UpdateAlarmDisabledState':
  if len(args) != 4:
    print 'UpdateAlarmDisabledState requires 4 args'
    sys.exit(1)
  pp.pprint(client.UpdateAlarmDisabledState(args[0],args[1],args[2],eval(args[3]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
